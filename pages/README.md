# pages/ directory — these files are no longer used as standalone pages.
# All pages are now built inline in app.py to avoid st.set_page_config() conflicts.
# This folder is kept for reference / future modular expansion.
